<% title "Administer Contact Status" %>

<ul class="sf-menu">
	<li ><%= link_to 'Create New Contact Status', new_contact_status_path %></li>
</ul>

<br><br>
<h3><%= @page_title %></h3>
<br>

<%= flash_messages %>

<table class="tableAdminKeywords">
  <tr>
    <th><b>Status Value</b></th>
  </tr>
  <% for contact_status in @contact_statuses %>
    <tr>
      <td><%= link_to contact_status.value, contact_status %></td>
      <!--
		    <td><%= link_to 'Show', contact_status %></td>
		    <td><%= link_to 'Edit', edit_contact_status_path(contact_status) %></td>
			-->
			<td><%= link_to '-delete-', contact_status, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>
<br>